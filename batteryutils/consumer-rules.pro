# BatteryUtils consumer rules.
# The library's public API surface is plain Java classes/enums with no
# reflection, serialization, or annotation-processing magic, so no special
# keep rules are required for consumers.
